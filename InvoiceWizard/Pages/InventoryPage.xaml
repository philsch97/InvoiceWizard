<Page x:Class="InvoiceWizard.InventoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="Materialpool">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Bestand und Kleinmaterial" Style="{StaticResource PageTitleStyle}"/>
                <TextBlock Text="Hier verwaltest du als Bestand oder als allgemeines Kleinmaterial markierte Materialpositionen, kannst sie durchsuchen, direkt Projekten zuweisen oder wieder in die normale Zuweisung zurueckgeben." Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <WrapPanel>
                <StackPanel Width="220" Margin="0,0,14,10">
                    <TextBlock Text="Anzeigen" Style="{StaticResource MutedLabelStyle}"/>
                    <ComboBox x:Name="PoolModeCombo" SelectionChanged="PoolModeCombo_SelectionChanged">
                        <ComboBoxItem Content="Bestand" Tag="inventory" IsSelected="True"/>
                        <ComboBoxItem Content="Allg. Kleinmaterial" Tag="generalSmallMaterial"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Width="260" Margin="0,0,14,10">
                    <TextBlock x:Name="SearchLabelText" Text="Bestand durchsuchen" Style="{StaticResource MutedLabelStyle}"/>
                    <TextBox x:Name="SearchTextBox" TextChanged="SearchTextBox_TextChanged"/>
                </StackPanel>
                <StackPanel Width="220" Margin="0,0,14,10">
                    <TextBlock Text="Kunde" Style="{StaticResource MutedLabelStyle}"/>
                    <ComboBox x:Name="CustomerCombo" DisplayMemberPath="Name" SelectedValuePath="CustomerId" SelectionChanged="CustomerCombo_SelectionChanged"/>
                </StackPanel>
                <StackPanel Width="220" Margin="0,0,14,10">
                    <TextBlock Text="Projekt" Style="{StaticResource MutedLabelStyle}"/>
                    <ComboBox x:Name="ProjectCombo" DisplayMemberPath="Name" SelectedValuePath="ProjectId"/>
                </StackPanel>
                <StackPanel Width="100" Margin="0,0,14,10">
                    <TextBlock Text="Menge" Style="{StaticResource MutedLabelStyle}"/>
                    <TextBox x:Name="AllocateQtyText" Text="1"/>
                </StackPanel>
                <StackPanel Width="140" Margin="0,0,14,10">
                    <TextBlock Text="Preis/Stk (optional)" Style="{StaticResource MutedLabelStyle}"/>
                    <TextBox x:Name="CustomerPriceText"/>
                </StackPanel>
                <Button x:Name="AllocateButton" Width="180" Height="40" Margin="0,24,10,0" Content="Aus Bestand zuweisen" Click="AllocateQuantity_Click"/>
                <Button Width="220" Height="40" Margin="0,24,10,0" Content="Markierte komplett zuweisen" Click="AllocateCompleteSelection_Click"/>
                <Button x:Name="RemoveButton" Width="190" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Aus Bestand nehmen" Click="RemoveFromPool_Click"/>
                <Button Width="110" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Aktualisieren" Click="Refresh_Click"/>
                <CheckBox x:Name="ShowCompletedCheckBox" Margin="0,32,0,0" Content="Vollstaendig entnommene anzeigen" Checked="ShowCompletedChanged" Unchecked="ShowCompletedChanged"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="PoolSectionTitleText" Text="Bestandspositionen" Style="{StaticResource SectionTitleStyle}" Margin="18,18,18,0"/>
                <DataGrid x:Name="LinesGrid" Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Extended">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Rechnung" Binding="{Binding InvoiceNumber}" Width="SizeToHeader" MinWidth="120"/>
                        <DataGridTextColumn Header="Beleg" Binding="{Binding ExpenseStatus}" Width="SizeToHeader" MinWidth="110"/>
                        <DataGridTextColumn Header="Pos." Binding="{Binding Position}" Width="SizeToHeader" MinWidth="60"/>
                        <DataGridTextColumn Header="Artikel" Binding="{Binding ArticleNumber}" Width="SizeToCells" MinWidth="120"/>
                        <DataGridTextColumn Header="EAN" Binding="{Binding Ean}" Width="SizeToCells" MinWidth="110"/>
                        <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="3*" MinWidth="380"/>
                        <DataGridTextColumn Header="Menge" Binding="{Binding Quantity}" Width="SizeToHeader" MinWidth="80"/>
                        <DataGridTextColumn x:Name="RemainingColumn" Header="Noch im Bestand" Binding="{Binding RemainingQuantity}" Width="SizeToHeader" MinWidth="120"/>
                        <DataGridTextColumn Header="Einheit" Binding="{Binding Unit}" Width="SizeToHeader" MinWidth="80"/>
                        <DataGridTextColumn Header="Gesamtnetto&#10;pro Einheit" Binding="{Binding EffectiveNetUnitPrice, StringFormat={}{0:N2} &#x20AC;}" Width="SizeToHeader" MinWidth="170"/>
                        <DataGridTextColumn Header="Gesamtbrutto&#10;pro Einheit" Binding="{Binding GrossUnitPrice, StringFormat={}{0:N2} &#x20AC;}" Width="SizeToHeader" MinWidth="160"/>
                        <DataGridTextColumn Header="Zugeordnet" Binding="{Binding AllocationSummary}" Width="2*" MinWidth="240"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border x:Name="StatusBorder" Grid.Row="3" Background="{StaticResource StatusInfoBackgroundBrush}" BorderBrush="{StaticResource StatusInfoBorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14,12">
            <TextBlock x:Name="StatusText" Foreground="{StaticResource StatusInfoTextBrush}" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Page>
