<Page x:Class="InvoiceWizard.ProjectContactsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="920" d:DesignWidth="1320"
      Title="ProjectContactsPage">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Projektdaten" Style="{StaticResource PageTitleStyle}"/>
                    <TextBlock Text="Lege Projekte hier an, bearbeite sie ueber den Projektdialog und pruefe Nutzer, Grundstueckseigentuemer und den aktuellen Projektstatus kompakt an einer Stelle." Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Projekt auswaehlen" Style="{StaticResource SectionTitleStyle}"/>
                    <Grid Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.15*"/>
                            <ColumnDefinition Width="1.35*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="Kunde" Style="{StaticResource MutedLabelStyle}"/>
                            <ComboBox x:Name="CustomerCombo" DisplayMemberPath="Name" SelectionChanged="CustomerCombo_SelectionChanged"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="0,0,12,0">
                            <TextBlock Text="Projekt" Style="{StaticResource MutedLabelStyle}"/>
                            <ComboBox x:Name="ProjectCombo" DisplayMemberPath="DisplayName" SelectionChanged="ProjectCombo_SelectionChanged"/>
                        </StackPanel>

                        <WrapPanel Grid.Column="2" VerticalAlignment="Bottom">
                            <Button Width="140" Height="40" Margin="0,0,10,10" Content="Projekt neu" Click="CreateProject_Click"/>
                            <Button Width="140" Height="40" Margin="0,0,10,10" Style="{StaticResource SecondaryButtonStyle}" Content="Projekt bearbeiten" Click="EditProject_Click"/>
                            <Button Width="140" Height="40" Margin="0,0,10,10" Style="{StaticResource SecondaryButtonStyle}" Content="Projekt loeschen" Click="DeleteProject_Click"/>
                            <Button Width="130" Height="40" Margin="0,0,0,10" Style="{StaticResource SecondaryButtonStyle}" Content="Aktualisieren" Click="Refresh_Click"/>
                        </WrapPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*"/>
                    <ColumnDefinition Width="0.9*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Projektuebersicht" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock x:Name="ProjectSummaryTitleText" Text="Kein Projekt ausgewaehlt" FontSize="24" FontWeight="SemiBold" Margin="0,16,0,0"/>
                        <TextBlock x:Name="ProjectSummaryMetaText" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,16" TextWrapping="Wrap"/>

                        <UniformGrid Columns="3" Margin="0,0,0,18">
                            <Border Background="#202730" CornerRadius="14" Padding="14" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="Status" Style="{StaticResource MutedLabelStyle}"/>
                                    <TextBlock x:Name="ProjectStatusText" FontSize="20" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#202730" CornerRadius="14" Padding="14" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="Offene Positionen" Style="{StaticResource MutedLabelStyle}"/>
                                    <TextBlock x:Name="OpenPositionsText" FontSize="20" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#202730" CornerRadius="14" Padding="14">
                                <StackPanel>
                                    <TextBlock Text="Offene Entwurfsrechnungen" Style="{StaticResource MutedLabelStyle}"/>
                                    <TextBlock x:Name="OpenDraftInvoicesText" FontSize="20" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <WrapPanel Margin="0,0,0,18">
                            <Button Width="120" Height="38" Margin="0,0,10,10" Content="Als aktiv" Click="SetProjectActive_Click"/>
                            <Button Width="120" Height="38" Margin="0,0,10,10" Style="{StaticResource SecondaryButtonStyle}" Content="Pausieren" Click="SetProjectPaused_Click"/>
                            <Button Width="120" Height="38" Margin="0,0,10,10" Style="{StaticResource SecondaryButtonStyle}" Content="Beenden" Click="SetProjectEnded_Click"/>
                        </WrapPanel>

                        <TextBlock x:Name="ProjectEndHintText" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Nutzer" Style="{StaticResource SectionTitleStyle}"/>
                            <TextBlock x:Name="ConnectionUserSummaryText" Margin="0,16,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Grundstueckseigentuemer" Style="{StaticResource SectionTitleStyle}"/>
                            <TextBlock x:Name="PropertyOwnerSummaryText" Margin="0,16,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <Border x:Name="StatusBorder" Grid.Row="3" Background="{StaticResource StatusInfoBackgroundBrush}" BorderBrush="{StaticResource StatusInfoBorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14,12" Margin="0,16,0,0">
                <TextBlock x:Name="StatusText" Foreground="{StaticResource StatusInfoTextBrush}" TextWrapping="Wrap"/>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
