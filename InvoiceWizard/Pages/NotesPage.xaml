<Page x:Class="InvoiceWizard.NotesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="820" d:DesignWidth="1200"
      Title="NotesPage">
    <Page.Resources>
        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <HierarchicalDataTemplate x:Key="TodoItemTemplate" ItemsSource="{Binding Children}">
            <Border Padding="4,2" CornerRadius="6" Background="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Path=Background}">
                <StackPanel Orientation="Horizontal">
                    <CheckBox IsChecked="{Binding IsCompleted}" Tag="{Binding TodoItemId}" Checked="TodoItemCheckBox_Changed" Unchecked="TodoItemCheckBox_Changed" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap" VerticalAlignment="Center" Foreground="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Path=Foreground}"/>
                </StackPanel>
            </Border>
        </HierarchicalDataTemplate>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Notizen und ToDos" Style="{StaticResource PageTitleStyle}"/>
                    <TextBlock Text="Erstelle Checklisten pro Kunde oder Projekt, hake erledigte Punkte ab und haenge Bilder direkt an die Notiz an." Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="260"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,14,0">
                        <TextBlock Text="Kunde" Style="{StaticResource MutedLabelStyle}"/>
                        <ComboBox x:Name="CustomerCombo" DisplayMemberPath="Name" SelectedValuePath="CustomerId" SelectionChanged="CustomerCombo_SelectionChanged"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,14,0">
                        <TextBlock Text="Projekt" Style="{StaticResource MutedLabelStyle}"/>
                        <ComboBox x:Name="ProjectCombo" DisplayMemberPath="Name" SelectedValuePath="ProjectId" SelectionChanged="ProjectCombo_SelectionChanged"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,0,14,0">
                        <TextBlock Text="Neue Liste" Style="{StaticResource MutedLabelStyle}"/>
                        <TextBox x:Name="TodoListTitleText"/>
                    </StackPanel>

                    <WrapPanel Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                        <Button Width="160" Height="40" Margin="0,0,10,0" Content="Liste anlegen" Click="CreateTodoList_Click"/>
                        <Button Width="120" Height="40" Style="{StaticResource SecondaryButtonStyle}" Content="Aktualisieren" Click="Refresh_Click"/>
                    </WrapPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="360"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,12">
                            <TextBlock Text="Listen" Style="{StaticResource SectionTitleStyle}" DockPanel.Dock="Left"/>
                            <Button Width="130" Height="34" HorizontalAlignment="Right" Style="{StaticResource SecondaryButtonStyle}" Content="Liste loeschen" Click="DeleteTodoList_Click"/>
                        </DockPanel>
                        <ListBox x:Name="TodoListsBox" SelectionChanged="TodoListsBox_SelectionChanged">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrushTheme}" BorderThickness="1" CornerRadius="12" Padding="12" Margin="0,0,0,10">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding ScopeLabel}" Foreground="{StaticResource MutedTextBrush}" Margin="0,6,0,0"/>
                                            <TextBlock Foreground="{StaticResource MutedTextBrush}" Margin="0,6,0,0">
                                                <Run Text="Offen: "/><Run Text="{Binding OpenItemCount}"/><Run Text=" | Erledigt: "/><Run Text="{Binding CompletedItemCount}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel>
                            <TextBlock x:Name="SelectedListTitleText" Text="Keine Liste ausgewaehlt" Style="{StaticResource SectionTitleStyle}"/>
                            <TextBlock x:Name="SelectedListMetaText" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                        </StackPanel>

                        <Grid Grid.Row="1" Margin="0,16,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="140"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TodoItemText" Margin="0,0,12,0" VerticalContentAlignment="Center"/>
                            <Button Grid.Column="1" Width="140" Height="38" Margin="0,0,12,0" Content="Punkt anlegen" Click="AddRootItem_Click"/>
                            <Button Grid.Column="2" Width="140" Height="38" Margin="0,0,12,0" Style="{StaticResource SecondaryButtonStyle}" Content="Unterpunkt" Click="AddChildItem_Click"/>
                            <Button Grid.Column="3" Width="130" Height="38" Style="{StaticResource SecondaryButtonStyle}" Content="Punkt loeschen" Click="DeleteTodoItem_Click"/>
                        </Grid>

                        <TreeView Grid.Row="2" x:Name="TodoTree" SelectedItemChanged="TodoTree_SelectedItemChanged" ItemTemplate="{StaticResource TodoItemTemplate}"/>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="10,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Bilder" Style="{StaticResource SectionTitleStyle}"/>

                        <TextBlock Grid.Row="1" Text="Beschriftung fuer neue Bilder" Style="{StaticResource MutedLabelStyle}" Margin="0,14,0,6"/>
                        <TextBox Grid.Row="2" x:Name="ImageCaptionText" Margin="0,0,0,14"/>

                        <Grid Grid.Row="3">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <WrapPanel Margin="0,0,0,14">
                                <Button Width="150" Height="38" Margin="0,0,10,0" Content="Bilder hinzufuegen" Click="UploadImage_Click"/>
                                <Button Width="150" Height="38" Style="{StaticResource SecondaryButtonStyle}" Content="Ausgewaehltes Bild loeschen" Click="DeleteSelectedImage_Click"/>
                            </WrapPanel>

                            <ListBox Grid.Row="1" x:Name="AttachmentsListBox" BorderThickness="0" Background="Transparent" SelectionChanged="AttachmentsListBox_SelectionChanged">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrushTheme}" BorderThickness="1" CornerRadius="12" Padding="10" Margin="0,0,0,10">
                                            <StackPanel>
                                                <Image Source="{Binding Preview}" Height="150" Stretch="UniformToFill"/>
                                                <TextBlock Text="{Binding FileName}" Margin="0,8,0,0" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Caption}" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,6,0,0" TextWrapping="Wrap" Visibility="{Binding CaptionVisibility}"/>
                                                <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <Border x:Name="StatusBorder" Grid.Row="3" Background="{StaticResource StatusInfoBackgroundBrush}" BorderBrush="{StaticResource StatusInfoBorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14,12" Margin="0,16,0,0">
                <TextBlock x:Name="StatusText" Foreground="{StaticResource StatusInfoTextBrush}" TextWrapping="Wrap"/>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
