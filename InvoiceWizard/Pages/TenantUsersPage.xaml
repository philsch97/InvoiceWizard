<Page x:Class="InvoiceWizard.TenantUsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="1080" d:DesignWidth="1320"
      Title="TenantUsersPage">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Benutzerverwaltung" Style="{StaticResource PageTitleStyle}"/>
                    <TextBlock x:Name="HeaderText" Margin="0,8,0,0" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Neue Person anlegen" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock x:Name="LicenseText" Margin="0,8,0,16" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Name" Style="{StaticResource MutedLabelStyle}"/>
                                <TextBox x:Name="CreateDisplayNameText"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="E-Mail-Adresse" Style="{StaticResource MutedLabelStyle}"/>
                                <TextBox x:Name="CreateEmailText"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Passwort" Style="{StaticResource MutedLabelStyle}"/>
                                <PasswordBox x:Name="CreatePasswordBox"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="Rolle" Style="{StaticResource MutedLabelStyle}"/>
                                <ComboBox x:Name="CreateRoleCombo"/>
                            </StackPanel>
                        </Grid>

                        <WrapPanel>
                            <Button Width="170" Height="40" Margin="0,0,10,0" Content="Benutzer anlegen" Click="CreateUser_Click"/>
                            <Button Width="130" Height="40" Style="{StaticResource SecondaryButtonStyle}" Content="Formular leeren" Click="ClearCreateForm_Click"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Ausgewaehlten Benutzer bearbeiten" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock x:Name="SelectionHintText" Margin="0,8,0,16" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Name" Style="{StaticResource MutedLabelStyle}"/>
                                <TextBox x:Name="EditDisplayNameText"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="E-Mail-Adresse" Style="{StaticResource MutedLabelStyle}"/>
                                <TextBox x:Name="EditEmailText" IsReadOnly="True"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Rolle" Style="{StaticResource MutedLabelStyle}"/>
                                <ComboBox x:Name="EditRoleCombo"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="Neues Passwort (optional)" Style="{StaticResource MutedLabelStyle}"/>
                                <PasswordBox x:Name="EditPasswordBox"/>
                            </StackPanel>
                        </Grid>

                        <CheckBox x:Name="EditIsActiveCheck" Content="Benutzer ist aktiv" Margin="0,0,0,16"/>

                        <WrapPanel>
                            <Button Width="190" Height="40" Margin="0,0,10,0" Content="Aenderungen speichern" Click="UpdateUser_Click"/>
                            <Button Width="120" Height="40" Margin="0,0,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Auswahl loesen" Click="ClearSelection_Click"/>
                            <Button Width="120" Height="40" Style="{StaticResource SecondaryButtonStyle}" Content="Aktualisieren" Click="Refresh_Click"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <Border x:Name="LicensingSectionBorder" Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16" Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="1.2*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Lizenz-Aktivierungscode erstellen" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock x:Name="LicenseAdminText" Margin="0,8,0,16" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>

                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Tarif" Style="{StaticResource MutedLabelStyle}"/>
                            <ComboBox x:Name="PlanCombo" DisplayMemberPath="DisplayLabel"/>
                        </StackPanel>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Kunden-E-Mail optional" Style="{StaticResource MutedLabelStyle}"/>
                                <TextBox x:Name="ActivationCustomerEmailText"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="Gueltig bis optional" Style="{StaticResource MutedLabelStyle}"/>
                                <DatePicker x:Name="ActivationValidUntilPicker"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Benutzerlimit optional" Style="{StaticResource MutedLabelStyle}"/>
                                <TextBox x:Name="MaxUsersOverrideText"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8,0,8,0">
                                <TextBlock Text="Projektlimit optional" Style="{StaticResource MutedLabelStyle}"/>
                                <TextBox x:Name="MaxProjectsOverrideText"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="8,0,0,0">
                                <TextBlock Text="Kundenlimit optional" Style="{StaticResource MutedLabelStyle}"/>
                                <TextBox x:Name="MaxCustomersOverrideText"/>
                            </StackPanel>
                        </Grid>

                        <CheckBox x:Name="MobileAccessOverrideCheck" Content="Mobilzugang aktivieren" Margin="0,0,0,16"/>

                        <WrapPanel>
                            <Button Width="170" Height="40" Margin="0,0,10,0" Content="Aktivierungscode erzeugen" Click="CreateLicenseActivation_Click"/>
                            <Button Width="140" Height="40" Style="{StaticResource SecondaryButtonStyle}" Content="Felder leeren" Click="ClearLicenseForm_Click"/>
                        </WrapPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Aktivierungscodes" Style="{StaticResource SectionTitleStyle}"/>
                        <DataGrid x:Name="LicenseActivationsGrid"
                                  Margin="0,16,0,0"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  SelectionMode="Single"
                                  HeadersVisibility="Column"
                                  MinHeight="250">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Code" Binding="{Binding ActivationCode}" Width="160"/>
                                <DataGridTextColumn Header="Tarif" Binding="{Binding PlanName}" Width="120"/>
                                <DataGridTextColumn Header="Limits" Binding="{Binding LimitsLabel}" Width="170"/>
                                <DataGridTextColumn Header="E-Mail" Binding="{Binding CustomerEmail}" Width="160"/>
                                <DataGridTextColumn Header="Gueltig bis" Binding="{Binding ValidUntil, StringFormat=dd.MM.yyyy}" Width="110"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding StatusLabel}" Width="150"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Firmenbenutzer" Style="{StaticResource SectionTitleStyle}"/>
                    <DataGrid x:Name="UsersGrid"
                              Margin="0,16,0,0"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionChanged="UsersGrid_SelectionChanged"
                              HeadersVisibility="Column"
                              MinHeight="280">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="2*"/>
                            <DataGridTextColumn Header="E-Mail" Binding="{Binding Email}" Width="2*"/>
                            <DataGridTextColumn Header="Rolle" Binding="{Binding Role}" Width="*"/>
                            <DataGridCheckBoxColumn Header="Aktiv" Binding="{Binding IsActive}" Width="90"/>
                            <DataGridCheckBoxColumn Header="Standard" Binding="{Binding IsDefault}" Width="100"/>
                            <DataGridTextColumn Header="Erstellt" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy HH:mm}" Width="140"/>
                            <DataGridTextColumn Header="Letzter Login" Binding="{Binding LastLoginAt, StringFormat=dd.MM.yyyy HH:mm}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <Border x:Name="StatusBorder" Grid.Row="4" Background="{StaticResource StatusInfoBackgroundBrush}" BorderBrush="{StaticResource StatusInfoBorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14,12">
                <TextBlock x:Name="StatusText" Foreground="{StaticResource StatusInfoTextBrush}" TextWrapping="Wrap"/>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
