<Page x:Class="InvoiceWizard.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="860" d:DesignWidth="1280"
      Title="Kalender">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Kalender" Style="{StaticResource PageTitleStyle}"/>
                <TextBlock Text="Wechsle zwischen Tages- und Wochenansicht. Ein Klick auf einen Tag oeffnet die Terminerstellung nur dann, wenn du deinen eigenen Kalender bearbeitest." Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <WrapPanel>
                <StackPanel Width="240" Margin="0,0,14,10">
                    <TextBlock Text="Kalender von" Style="{StaticResource MutedLabelStyle}"/>
                    <ComboBox x:Name="UserCombo" DisplayMemberPath="DisplayName" SelectionChanged="UserCombo_SelectionChanged"/>
                </StackPanel>
                <StackPanel Width="180" Margin="0,0,14,10">
                    <TextBlock Text="Tag" Style="{StaticResource MutedLabelStyle}"/>
                    <DatePicker x:Name="SelectedDatePicker" SelectedDateChanged="SelectedDatePicker_SelectedDateChanged"/>
                </StackPanel>
                <Button Width="130" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Tagansicht" Click="DayView_Click"/>
                <Button Width="130" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Wochenansicht" Click="WeekView_Click"/>
                <Button Width="120" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Zurueck" Click="PreviousRange_Click"/>
                <Button Width="120" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Weiter" Click="NextRange_Click"/>
                <Button Width="120" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Heute" Click="Today_Click"/>
                <Button Width="120" Height="40" Margin="0,24,0,0" Content="Aktualisieren" Click="Refresh_Click"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock x:Name="CalendarModeInfoText" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock x:Name="SelectedCalendarInfoText" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="3" Margin="0,0,0,16">
            <Border x:Name="WeekCardsBorder" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock x:Name="WeekRangeText" Text="Woche" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock x:Name="CalendarCardsHintText" Text="Termine werden direkt in den Tageskarten angezeigt. Ein Klick auf einen Termin zeigt die Details." Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,16" TextWrapping="Wrap"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" MaxHeight="620">
                        <ItemsControl x:Name="WeekDaysItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel ItemWidth="220" ItemHeight="320"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrushTheme}" BorderThickness="1" CornerRadius="14" Padding="12" Margin="0,0,12,12" Width="208" Height="308">
                                        <StackPanel>
                                            <DockPanel LastChildFill="False">
                                                <Button Click="WeekDayCard_Click" Tag="{Binding Date}" Style="{StaticResource SecondaryButtonStyle}" Height="34" MinWidth="112" DockPanel.Dock="Left" Content="{Binding Title}"/>
                                                <Button Click="NewEntryForDay_Click" Tag="{Binding Date}" Width="34" Height="34" Margin="8,0,0,0" Content="+" Visibility="{Binding NewEntryButtonVisibility}" DockPanel.Dock="Right"/>
                                            </DockPanel>
                                            <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,12"/>
                                            <TextBlock Text="{Binding EmptyText}" Foreground="{StaticResource MutedTextBrush}" Visibility="{Binding EmptyVisibility}" Margin="0,4,0,0"/>
                                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="228">
                                            <ItemsControl ItemsSource="{Binding Entries}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Click="CalendarEntryCard_Click" Tag="{Binding Entry}" Margin="0,0,0,8" Padding="0" Background="Transparent" BorderThickness="0">
                                                            <Border Background="#16202A" BorderBrush="{StaticResource BorderBrushTheme}" BorderThickness="1" CornerRadius="10" Padding="8">
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding Primary}" TextWrapping="Wrap"/>
                                                                    <TextBlock Text="{Binding Secondary}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                                                    <TextBlock Text="{Binding Tertiary}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" TextWrapping="Wrap" Visibility="{Binding TertiaryVisibility}"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </Button>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            </ScrollViewer>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <ScrollViewer x:Name="DayCardScrollViewer" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" MaxHeight="560" Visibility="Collapsed">
                        <ItemsControl x:Name="DayCardsItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrushTheme}" BorderThickness="1" CornerRadius="14" Padding="16">
                                            <StackPanel>
                                                <DockPanel LastChildFill="False">
                                                    <Button Click="WeekDayCard_Click" Tag="{Binding Date}" Style="{StaticResource SecondaryButtonStyle}" Height="36" MinWidth="140" DockPanel.Dock="Left" Content="{Binding Title}"/>
                                                    <Button Click="NewEntryForDay_Click" Tag="{Binding Date}" Width="160" Height="36" Margin="10,0,0,0" Content="Termin an diesem Tag" Visibility="{Binding NewEntryButtonVisibility}" DockPanel.Dock="Right"/>
                                                </DockPanel>
                                                <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,16"/>
                                                <TextBlock Text="{Binding EmptyText}" Foreground="{StaticResource MutedTextBrush}" Visibility="{Binding EmptyVisibility}" Margin="0,4,0,0"/>
                                                <ItemsControl ItemsSource="{Binding Entries}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Button Click="CalendarEntryCard_Click" Tag="{Binding Entry}" Margin="0,0,0,10" Padding="0" Background="Transparent" BorderThickness="0">
                                                                <Border Background="#16202A" BorderBrush="{StaticResource BorderBrushTheme}" BorderThickness="1" CornerRadius="10" Padding="10">
                                                                    <StackPanel>
                                                                        <TextBlock Text="{Binding Primary}" TextWrapping="Wrap"/>
                                                                        <TextBlock Text="{Binding Secondary}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                                                        <TextBlock Text="{Binding Tertiary}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" TextWrapping="Wrap" Visibility="{Binding TertiaryVisibility}"/>
                                                                    </StackPanel>
                                                                </Border>
                                                            </Button>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="4" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Termindetails" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock x:Name="EntryDetailsTitleText" Margin="0,12,0,0" FontWeight="SemiBold" Text="Noch kein Termin ausgewaehlt."/>
                <TextBlock x:Name="EntryDetailsMetaText" Margin="0,6,0,0" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>
                <TextBlock x:Name="EntryDetailsCustomerText" Margin="0,12,0,0" TextWrapping="Wrap"/>
                <TextBlock x:Name="EntryDetailsAddressText" Margin="0,6,0,0" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>
                <TextBlock x:Name="EntryDetailsDescriptionText" Margin="0,12,0,0" TextWrapping="Wrap"/>
                <WrapPanel Margin="0,16,0,0">
                    <Button x:Name="DetailEditButton" Width="120" Height="36" Margin="0,0,10,0" Content="Bearbeiten" Click="EditSelectedEntry_Click"/>
                    <Button x:Name="DetailDeleteButton" Width="120" Height="36" Style="{StaticResource SecondaryButtonStyle}" Content="Loeschen" Click="DeleteSelectedEntry_Click"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border x:Name="StatusBorder" Grid.Row="5" Background="{StaticResource StatusInfoBackgroundBrush}" BorderBrush="{StaticResource StatusInfoBorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14,12">
            <TextBlock x:Name="StatusText" Foreground="{StaticResource StatusInfoTextBrush}" TextWrapping="Wrap"/>
        </Border>
    </Grid>
    </ScrollViewer>
</Page>
