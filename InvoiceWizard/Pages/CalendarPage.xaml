<Page x:Class="InvoiceWizard.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="860" d:DesignWidth="1280"
      Title="Kalender">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Kalender" Style="{StaticResource PageTitleStyle}"/>
                <TextBlock Text="Jeder Benutzer sieht seinen Kalender, kann aber auch die Kalender der anderen Kollegen lesen. Die Wochenansicht zeigt alle Termine der Firma gemeinsam." Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <WrapPanel>
                <StackPanel Width="240" Margin="0,0,14,10">
                    <TextBlock Text="Kalender von" Style="{StaticResource MutedLabelStyle}"/>
                    <ComboBox x:Name="UserCombo" DisplayMemberPath="DisplayName" SelectionChanged="UserCombo_SelectionChanged"/>
                </StackPanel>
                <StackPanel Width="180" Margin="0,0,14,10">
                    <TextBlock Text="Tag" Style="{StaticResource MutedLabelStyle}"/>
                    <DatePicker x:Name="SelectedDatePicker" SelectedDateChanged="SelectedDatePicker_SelectedDateChanged"/>
                </StackPanel>
                <Button Width="120" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Woche zurueck" Click="PreviousWeek_Click"/>
                <Button Width="120" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Naechste Woche" Click="NextWeek_Click"/>
                <Button Width="120" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Heute" Click="Today_Click"/>
                <Button Width="120" Height="40" Margin="0,24,0,0" Content="Aktualisieren" Click="Refresh_Click"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*"/>
                    <ColumnDefinition Width="0.9*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Margin="0,0,16,0">
                    <TextBlock Text="Termine des gewaehlten Tages" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock x:Name="SelectedCalendarInfoText" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    <DataGrid x:Name="UserEntriesGrid" Margin="0,16,0,0" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" SelectionChanged="UserEntriesGrid_SelectionChanged" MinHeight="220">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Zeit" Binding="{Binding TimeRange}" Width="110"/>
                            <DataGridTextColumn Header="Titel" Binding="{Binding Title}" Width="180"/>
                            <DataGridTextColumn Header="Ort" Binding="{Binding Location}" Width="150"/>
                            <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Termin bearbeiten" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock x:Name="EditorInfoText" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>

                    <Grid Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,14">
                            <TextBlock Text="Titel * Pflicht" Style="{StaticResource RequiredLabelStyle}"/>
                            <TextBox x:Name="TitleText"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,14,14">
                            <TextBlock Text="Datum * Pflicht" Style="{StaticResource RequiredLabelStyle}"/>
                            <DatePicker x:Name="EntryDatePicker"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,14">
                            <TextBlock Text="Ort optional" Style="{StaticResource MutedLabelStyle}"/>
                            <TextBox x:Name="LocationText"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,14,14">
                            <TextBlock Text="Start * Pflicht" Style="{StaticResource RequiredLabelStyle}"/>
                            <TextBox x:Name="StartTimeText" Text="08:00"/>
                        </StackPanel>
                        <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,14">
                            <TextBlock Text="Ende * Pflicht" Style="{StaticResource RequiredLabelStyle}"/>
                            <TextBox x:Name="EndTimeText" Text="09:00"/>
                        </StackPanel>

                        <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
                            <TextBlock Text="Beschreibung optional" Style="{StaticResource MutedLabelStyle}"/>
                            <TextBox x:Name="DescriptionText" AcceptsReturn="True" TextWrapping="Wrap" Height="100" VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                    </Grid>

                    <WrapPanel Margin="0,18,0,0">
                        <Button Width="150" Height="40" Margin="0,0,10,0" Content="Termin speichern" Click="SaveEntry_Click"/>
                        <Button Width="130" Height="40" Margin="0,0,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Neu" Click="NewEntry_Click"/>
                        <Button Width="130" Height="40" Style="{StaticResource SecondaryButtonStyle}" Content="Loeschen" Click="DeleteEntry_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="WeekOverviewTitleText" Text="Wochenuebersicht aller Benutzer" Style="{StaticResource SectionTitleStyle}" Margin="18,18,18,0"/>
                <DataGrid x:Name="WeekOverviewGrid" Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tag" Binding="{Binding DayLabel}" Width="120"/>
                        <DataGridTextColumn Header="Zeit" Binding="{Binding TimeRange}" Width="110"/>
                        <DataGridTextColumn Header="Benutzer" Binding="{Binding UserDisplayName}" Width="150"/>
                        <DataGridTextColumn Header="Titel" Binding="{Binding Title}" Width="180"/>
                        <DataGridTextColumn Header="Ort" Binding="{Binding Location}" Width="160"/>
                        <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border x:Name="StatusBorder" Grid.Row="4" Background="{StaticResource StatusInfoBackgroundBrush}" BorderBrush="{StaticResource StatusInfoBorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14,12">
            <TextBlock x:Name="StatusText" Foreground="{StaticResource StatusInfoTextBrush}" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Page>
