<Page x:Class="InvoiceWizard.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="860" d:DesignWidth="1280"
      Title="Kalender">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Kalender" Style="{StaticResource PageTitleStyle}"/>
                <TextBlock Text="Wechsle zwischen Tages- und Wochenansicht. Ein Klick auf einen Tag oeffnet die Terminerstellung nur dann, wenn du deinen eigenen Kalender bearbeitest." Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <WrapPanel>
                <StackPanel Width="240" Margin="0,0,14,10">
                    <TextBlock Text="Kalender von" Style="{StaticResource MutedLabelStyle}"/>
                    <ComboBox x:Name="UserCombo" DisplayMemberPath="DisplayName" SelectionChanged="UserCombo_SelectionChanged"/>
                </StackPanel>
                <StackPanel Width="180" Margin="0,0,14,10">
                    <TextBlock Text="Tag" Style="{StaticResource MutedLabelStyle}"/>
                    <DatePicker x:Name="SelectedDatePicker" SelectedDateChanged="SelectedDatePicker_SelectedDateChanged"/>
                </StackPanel>
                <Button Width="130" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Tagansicht" Click="DayView_Click"/>
                <Button Width="130" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Wochenansicht" Click="WeekView_Click"/>
                <Button Width="120" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Zurueck" Click="PreviousRange_Click"/>
                <Button Width="120" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Weiter" Click="NextRange_Click"/>
                <Button Width="120" Height="40" Margin="0,24,10,0" Style="{StaticResource SecondaryButtonStyle}" Content="Heute" Click="Today_Click"/>
                <Button Width="120" Height="40" Margin="0,24,0,0" Content="Aktualisieren" Click="Refresh_Click"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock x:Name="CalendarModeInfoText" Style="{StaticResource SectionTitleStyle}"/>
                <TextBlock x:Name="SelectedCalendarInfoText" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="3" Margin="0,0,0,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border x:Name="WeekCardsBorder" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock x:Name="WeekRangeText" Text="Woche" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBlock Text="Klicke auf einen Tag, um ihn zu oeffnen. In deinem eigenen Kalender startet dadurch direkt der Termin-Dialog." Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,16" TextWrapping="Wrap"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                        <ItemsControl x:Name="WeekDaysItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1" Columns="7"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Click="WeekDayCard_Click" Tag="{Binding Date}" Margin="0,0,10,0" Padding="0" Background="Transparent" BorderThickness="0">
                                        <Border Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrushTheme}" BorderThickness="1" CornerRadius="14" Padding="12" MinWidth="155" Height="240">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,12"/>
                                                <ItemsControl ItemsSource="{Binding PreviewLines}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="#16202A" BorderBrush="{StaticResource BorderBrushTheme}" BorderThickness="1" CornerRadius="10" Padding="8" Margin="0,0,0,8">
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding Primary}" TextWrapping="Wrap"/>
                                                                    <TextBlock Text="{Binding Secondary}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                                                </StackPanel>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel Margin="18,18,18,0">
                        <TextBlock x:Name="DayEntriesTitleText" Style="{StaticResource SectionTitleStyle}" DockPanel.Dock="Left"/>
                        <Button x:Name="NewEntryButton" Width="170" Height="40" DockPanel.Dock="Right" Content="Termin an diesem Tag" Click="NewEntry_Click"/>
                    </DockPanel>
                    <DataGrid x:Name="DayEntriesGrid" Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Zeit" Binding="{Binding TimeRange}" Width="110"/>
                            <DataGridTextColumn Header="Titel" Binding="{Binding Title}" Width="190"/>
                            <DataGridTextColumn Header="Ort" Binding="{Binding Location}" Width="160"/>
                            <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="*"/>
                            <DataGridTemplateColumn Header="Aktionen" Width="210">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Width="90" Height="34" Margin="0,0,8,0" Content="Bearbeiten" Click="EditEntry_Click" Tag="{Binding}" IsEnabled="{Binding CanEdit}"/>
                                            <Button Width="90" Height="34" Style="{StaticResource SecondaryButtonStyle}" Content="Loeschen" Click="DeleteEntry_Click" Tag="{Binding}" IsEnabled="{Binding CanEdit}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Border x:Name="StatusBorder" Grid.Row="4" Background="{StaticResource StatusInfoBackgroundBrush}" BorderBrush="{StaticResource StatusInfoBorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14,12">
            <TextBlock x:Name="StatusText" Foreground="{StaticResource StatusInfoTextBrush}" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Page>
